MRA_Shop_Board_v6_9_7.xlsx
fleetio.txt
samsara.txt
*-debug.txt
fleetio-loc-debug.txt
fleetio-sync-log.txt

# Local export secrets / diagnostics (never commit)
azure-key.txt
samsara.txt
export-log.txt
export-last-run.txt
shoptasks-unmatched.txt
# Cloud-export job: the workbook copy it downloads into the runner
workbook.xlsx
lists.json
data.fromlists.js
__pycache__/
*.pyc

# Local-only debug scaffold used to expose Three.js internals (scanG/camera/scanParts) to
# headless test scripts while diagnosing the 3D Builder -- never a real deliverable, recreated
# and deleted repeatedly during a debugging session, so it's ignored rather than committed.
builder/_debug_test_only.html

# Local symlink to the system-installed Playwright package, recreated as needed for headless
# verification scripts -- this repo has no build step / package.json, so it's test scaffolding
# only, not a real dependency.
node_modules/
