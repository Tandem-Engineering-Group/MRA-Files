{
  "name": "camera-gear-dash",
  "version": "1.0.0",
  "private": true,
  "description": "Stop·Down — live used-camera-gear search desk with daily alerts (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
